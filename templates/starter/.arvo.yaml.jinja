# Arvo Project Configuration
# This file tracks the arvo version and installed cartridges

arvo_version: "0.1.0"
created_at: "{{ _copier_conf.timestamp }}"
project_name: "{{ project_name }}"
project_slug: "{{ project_slug }}"

# Installed cartridges (managed by 'arvo add' and 'arvo remove')
cartridges: []

