# Production Environment Configuration
# =====================================
# Copy this file to .env.prod and configure all values
# NEVER commit .env.prod to version control!
#
# Generate secret keys with: just secret

# ============================================================
# APPLICATION
# ============================================================

APP_NAME="Agency Standard"
ENVIRONMENT=production
DEBUG=false

# REQUIRED: Generate with `just secret` or `python -c "import secrets; print(secrets.token_urlsafe(32))"`
SECRET_KEY=

# ============================================================
# DOMAIN & SSL
# ============================================================

# Your API domain (without https://)
DOMAIN=api.example.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com

# ============================================================
# DATABASE
# ============================================================

# PostgreSQL credentials
DB_USER=agency_prod
DB_PASSWORD=  # REQUIRED: Use a strong password
DB_NAME=agency_production

# Full database URL (used internally by the app)
# This is automatically constructed in docker-compose.prod.yml
# DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# ============================================================
# REDIS
# ============================================================

# Redis URL (used internally by the app)
# This is automatically constructed in docker-compose.prod.yml
# REDIS_URL=redis://redis:6379

# ============================================================
# AUTHENTICATION
# ============================================================

# JWT Configuration
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================
# OAUTH PROVIDERS (Optional)
# ============================================================

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Microsoft OAuth
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth callback URL (update with your domain)
OAUTH_CALLBACK_URL=https://api.example.com/auth/oauth

# ============================================================
# RATE LIMITING
# ============================================================

RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# ============================================================
# OBSERVABILITY (Optional)
# ============================================================

# OpenTelemetry endpoint for tracing (e.g., Jaeger, Honeycomb)
OTLP_ENDPOINT=

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================
# CORS (Optional)
# ============================================================

# Allowed origins for CORS (comma-separated)
# CORS_ORIGINS=https://app.example.com,https://www.example.com

# ============================================================
# SECURITY NOTES
# ============================================================
#
# 1. SECRET_KEY: Must be unique and random. Never reuse across environments.
# 2. DB_PASSWORD: Use a strong, unique password (min 32 characters recommended).
# 3. OAuth secrets: Keep these confidential and rotate periodically.
# 4. Never expose this file publicly or commit to version control.
# 5. Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault)
#    for production deployments.
#

