# Tach - Architecture boundary enforcement
# For the Arvo CLI (src/arvo/)

exact = true
source_roots = ["src"]
exclude = [
    "tests",
    "templates",
    "cartridges",
]

# Arvo CLI module - simple structure, no strict layering needed
[[modules]]
path = "arvo"
depends_on = ["arvo.commands"]

[[modules]]
path = "arvo.commands"
depends_on = ["arvo"]
